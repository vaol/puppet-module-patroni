# This file can be used to install module dependencies for unit testing
# See https://github.com/puppetlabs/puppetlabs_spec_helper#using-fixtures for details
---
fixtures:
  forge_modules:
    stdlib:
      repo: 'puppetlabs/stdlib'
      ref: '9.6.0'
    postgres:
      repo: 'puppetlabs-postgresql'
      ref: '7.2.0'
    apt:
      repo: 'puppetlabs-apt'
      ref: '9.4.0'
    python:
      repo: 'puppet-python'
      ref: '7.3.0'
    epel:
      repo: 'puppet-epel'
      ref: '5.0.0'
    systemd:
      repo: 'puppet-systemd'
      ref: '7.0.0'
    yumrepo:
      repo: 'puppetlabs-yumrepo_core'
      ref: '2.1.0'

  #repositories:
  #  postgresql:
  #    repo: "https://github.com/vaol/puppetlabs-postgresql.git"
  #    branch: 'upgrade_puppet_8'

  symlinks:
    patroni: "#{source_dir}"
